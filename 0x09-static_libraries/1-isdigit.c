#include "main.h"
/**
 * _isdigit - a function to check for digits 0-9
 * @c: int to be checked
 * Return: 1 if c is a digit and 0 if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
